# Python bytecode
__pycache__/
*.pyc

# Inferred database — a build artifact, regenerable from the upstream dump via
# scripts/build_db.py. Kept out of git: ~58 MB binary that would churn on every
# monthly rebuild. Run the skill's `init` step (or build_db.py) to generate it.
data/protondb.sqlite

# Raw upstream ProtonDB dump (cached outside the repo by build_db.py)
*.tar.gz
reports_piiremoved.json
