# JSM config — 8BitDo Ultimate 2 Wireless (DInput mode via SDL3)
# Matches user's Steam Input layout.
# Adopted: 2026-04-20 (Branch B), promoted to Branch A 2026-04-22. See findings/jsm_sdl3_verified.md.
#
# Invocation: JoyShockMapper.exe "<path to this file>"
# Requires JSM built from the branch-a-port branch (11 fork cherry-picks + 3 local
# patches — SDL3 release-3.4.4 pin, touchpad guard, and vendor-switch fixes).
# See findings/jsm_sdl3_verified.md §"Branch A achieved" for SHA list.

RESET_MAPPINGS

# --- Face buttons ---
N =
E =
S =
W =

# --- D-pad ---
UP =
DOWN =
LEFT =
RIGHT =

# --- Shoulders / triggers ---
L =
R =
ZL =
ZR =

# --- Stick clicks ---
L3 =
R3 =

# --- System buttons ---
- =
+ =
HOME =

# --- Rear paddles & mini-shoulders (Branch A — Ultimate 2 type case) ---
# Active values: assign your preferred keys. Routed via SDLWrapper.cpp:620-626.
LMINI =   # physical L4  (rear mini-shoulder, left)
RMINI =   # physical R4  (rear mini-shoulder, right)
LSL =     # physical PL  (paddle, left)
RSR =     # physical PR  (paddle, right)
# CAPTURE: not present on the Ultimate 2 Wireless (no physical capture button).

# --- Gyro ---
# "Precision speed" (Steam Input) ≈ GYRO_SENS scaling factor
GYRO_SENS = 2
# "Movement threshold" ≈ GYRO_CUTOFF_RECOVERY
GYRO_CUTOFF_RECOVERY = 0
# "Smooth fine movements" ≈ GYRO_SMOOTH_THRESHOLD
GYRO_SMOOTH_THRESHOLD = 0

# Gyro toggle: tap-to-disable (gyro is always-on by default in JSM, so we invert).
# GYRO_ON bound to a button means "gyro is on ONLY while that button is held" —
# not what we want. ^GYRO_OFF is a tap-toggle of the disable state.
HOME = ^GYRO_OFF

# Default axes (these match JSM defaults; listed for clarity):
# MOUSE_X_FROM_GYRO_AXIS = Y   # yaw → mouse X
# MOUSE_Y_FROM_GYRO_AXIS = X   # pitch → mouse Y (natural aim-style, not inverted)
